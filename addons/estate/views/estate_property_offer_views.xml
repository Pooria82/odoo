<odoo>
    <!-- Form View -->
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offers" editable="top"
            decoration-success="status=='accepted'"
            decoration-danger="status=='refused'">
                <field name="price" width="150"/>
                <field name="partner_id" width="150"/>
                <field name="validity" width="150"/>
                <field name="date_deadline" width="150"/>
<!--                <field name="status" width="150"/>-->
                <button name="action_accept_offer"
                        type="object"
                        icon="fa-check-circle"
                        class="btn btn-success"
                        title="Accept Offer"
                        invisible="status == 'accepted'"/>
                <button name="action_refuse_offer"
                        type="object"
                        icon="fa-times-circle"
                        class="btn btn-danger"
                        title="Refuse Offer"
                        invisible="status == 'refused'"/>
            </list>
        </field>
    </record>
</odoo>
