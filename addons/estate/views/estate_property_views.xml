<odoo>
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_filter_available': True}</field>
    </record>

    <!-- List View -->
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                  decoration-success="state in ('offer_received',  'offer_accepted')"
                  decoration-bf="state=='offer_accepted'"
                  decoration-muted="state ==  'sold'">
                <field name="name" width="150"/>
                <field name="property_type_id" width="150"/>
                <field name="postcode" width="150"/>
                <field name="tag_ids" widget="many2many_tags" width="100" options="{'color_field' : 'color'}"/>
                <field name="bedrooms" width="100"/>
                <field name="living_area" width="150"/>
                <field name="expected_price" width="150"/>
                <field name="selling_price" width="150"/>
                <field name="date_availability" width="150" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Properties">
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            class="btn btn-secondary"
                            invisible="state in ('canceled', 'sold')"/>
                    <button name="action_sold" type="object" string="Sold"
                            class="btn btn-primary"
                            invisible="state in ('canceled', 'sold')"/>
                </header>

                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field' : 'color'}"/>
                    </div>

                    <div class="spacer" style="margin-bottom: 20px;"></div>

                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create' : true, 'no_open' : true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                                <field name="state"/>
                                <field name="active"/>
                            </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'canceled')"/>
                        </page>

                        <page string="Other Info">
                            <group>
                                <field name="user_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties Search">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <field name="tag_ids"/>

                <separator/>

                <filter string="Available" name="filter_available"
                        domain="[('state', 'in', ['new', 'offer_received'])]"/>

                <group expand="1" string="Group By">
                    <filter string="Postcode" name="group_by_postcode"
                            context="{'group_by':'postcode'}"/>
                    <filter string="Property Type" name="group_by_property_type"
                            context="{'group_by':'property_type_id'}"/>
                    <filter string="Tags" name="group_by_tags"
                            context="{'group_by':'tag_ids'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
